package layout

templ Header() {
    @HeaderStyle()
    <div class="header">
        @Menu()
    </div>
}

templ HeaderStyle() {
    <style>
        .header {
            height: 150px;
            background-color: #ffffff;
            margin: 0px;
            padding: 5px;
            display: flex;
            flex-direction: column;
        }
    </style>
}